node_modules/
*.log
.env
.env.*
!.env.example
data/
!data/
data/*
!data/rx/
data/rx/*
!data/rx/config.json
!data/rx/names.json
.aider*
_snapshots/
*.bak-*
output/
.claude/settings.local.json

# Packed extension build artifacts - regenerate with scripts/pack-beswift-extension.ps1.
# The signing key lives in data/ext/ (already ignored via data/*) and must be
# backed up: losing it changes the extension ID and breaks the Edge policy.
public/extensions/*.crx
public/extensions/*-updates.xml
docs/setup.md
